name: docker
services:
  mysql_db:
    image: docker-mysql_db:latest
    container_name: mysql_db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: batbotkey
    ports:
      - "33306:3306"
    volumes:
      - docker_mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$${MYSQL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 10

  java_app:
    image: docker-java_app:latest
    container_name: java_app
    depends_on:
      mysql_db:
        condition: service_healthy
    ports:
      - "18080:8080"
    environment:
      MYSQL_HOST: mysql_db
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql_db:3306/batbotkey
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
      SPRING_FLYWAY_URL: jdbc:mysql://mysql_db:3306/batbotkey

  angular_app:
    image: docker-angular_app:latest
    container_name: angular_app
    ports:
      - "14200:4200"
    depends_on:
      java_app:
        condition: service_started

volumes:
  docker_mysql_data:
